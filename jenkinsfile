pipeline {
   agent { label 'master' }
   stages {
      stage ("Installing Dependencies on Slave Nodes"){
         steps {
	    sh "ansible-playbook jenkins_slave_dependency.yml -i inventory"
	 }
      }
   }
}
