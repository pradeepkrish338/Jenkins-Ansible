pipeline {
   agent { label 'master' }
   stages {
      stage ("Installing Dependencies on Slave Nodes:"){
         steps {
	    sh "ansible-playbook -i inventory jenkins_slave_dependency.yml"
	 }
      }
   }
}
